---
description: GitHub Actions is the ONLY compiler; download binaries via 10811 proxy
alwaysApply: true
---

# GitHub Actions 编译与本机下载

本机磁盘不足。编译规则见 `no-local-compile.mdc`（第一优先级、无例外）。本文件只写 **Actions 产物怎么下、怎么部署**。

## 仓库与产物

| 仓库 | Workflow | 产物 artifact 名 | 用途 |
|------|----------|------------------|------|
| `artifact-keeper` | `Build Release Binaries`（UBI9 容器内编译） | `artifact-keeper-linux-amd64` | 替换 Linux AMD64 / UBI9 容器内 `/usr/local/bin/artifact-keeper`（勿用 ubuntu-latest 裸编，glibc 不兼容 UBI9） |
| `artifact-keeper-cli` | `Build CLI Binaries` | `ak-windows-amd64` / `ak-linux-amd64` | 本机联调用 `ak.exe` |

触发：`workflow_dispatch`，或 push 到 `feat/**` / `fix/**`。

## 下载（必须走本机 10811 代理）

```bash
export HTTPS_PROXY=http://127.0.0.1:10811
export HTTP_PROXY=http://127.0.0.1:10811

# 用 git credential / GH_TOKEN 鉴权后拉取 Actions artifact zip
curl -fsSL -L -x http://127.0.0.1:10811 \
  -H "Authorization: Bearer $GH_TOKEN" \
  -H "Accept: application/vnd.github+json" \
  -o artifact.zip \
  "https://api.github.com/repos/<owner>/<repo>/actions/artifacts/<id>/zip"
```

或：

```bash
HTTPS_PROXY=http://127.0.0.1:10811 HTTP_PROXY=http://127.0.0.1:10811 \
  gh run download <run-id> -R <owner>/<repo> -n <artifact-name>
```

下载后校验 sha256（UI 上显示的往往是 **artifact zip** 的摘要，不一定是解压后的 ELF）。

## 部署 Linux AMD64（UBI9 后端容器）

SSH 目标以本机运维配置为准（勿把主机商品名写进 Release 说明）。示例：

```bash
scp artifact-keeper-linux-amd64 <host>:/tmp/artifact-keeper
ssh <host> 'docker cp /tmp/artifact-keeper artifact-keeper-backend:/usr/local/bin/artifact-keeper && docker restart artifact-keeper-backend'
```

## Agent 行为

1. **禁止**本机 `cargo` / `rustc`（含 debug、test、clippy）。
2. 改完需要联调的代码 → commit + push 到 `feat/**`，等 Actions 绿。
3. 用 **10811 代理**下载 artifact，再部署/替换本机 `ak`。
